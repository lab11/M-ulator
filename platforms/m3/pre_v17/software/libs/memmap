MEMORY
{
	RRAM (wx) : ORIGIN = 0, LENGTH = 8K
}

SECTIONS
{
	/* .vectors : { KEEP(*(.vectors)) } > RRAM */
	.text :
	{
		KEEP(*(.vectors))
		*(.text)
	} > RRAM
	
	.data : { *(.data) } > RRAM
	_bss_start = .;
	.bss : { *.(bss) } > RRAM
	_bss_end = .;
}
